<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/progressbar_background"
                android:centerColor="@color/progressbar_background"
                android:centerY="1.0"
                android:endColor="@color/progressbar_background"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/accent"
                    android:centerColor="@color/accent"
                    android:centerY="1.0"
                    android:endColor="@color/accent"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>